<div id="show-page-posts">
  <div class="showing-posts">
    <div class="created-at">
      <%= @codes.created_at.to_date %>
    </div>
    <div class="title">
      <%= @codes.title %>
    </div>
    <div class="content">
      <%= @codes.content %>
    </div>
  </div>
  <div class="edit-delete-actions">
    <% if signed_in? %>
      <%= link_to "Edit", edit_users_code_path(@codes.id), class: "edit-button" %>
      <%= link_to "Delete", users_code_path(@codes.id), method: :delete, 
        data: { confirm: "Are you sure you want to delete this post?" }, class: "delete-button" 
      %>
    <% end %>
  </div>
</div>
